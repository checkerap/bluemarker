<% if attendees.present? %>
  <div class="card">
    <div class="card-header d-flex justify-content-between">
      <div>
        <span class="h6">
          <%= title %>
        </span>
      </div>
      <% unless view_all %>
        <a href="/events/<%= event.id %>/attendees">View all ›</a>
      <% end %>
    </div>
    <div class="card-body">
      <ul class="list-unstyled list-spacing-sm">
        
        <% attendees.order(:name => :asc).each do |attendee| %>
        <li>
          <a class="media" href="/users/<%= attendee.id %>">
            <%= render partial: 'users/user_image', :locals => { user: attendee, image_classes: 'avatar avatar-sm mr-3' } %>
          
            <div class="media-body">
              <span class="h6 mb-0"><%= attendee.name %></span>
              <span class="text-muted"><%= attendee.title_organization %></span>
            </div>
          </a>
        </li>
        <% end %>

      </ul>
    </div>
  </div>
<% end %>