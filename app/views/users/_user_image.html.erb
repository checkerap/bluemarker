<% if user.image.attached? %>
  <%= image_tag url_for(user.image.variant(resize: "400x400!")), :class => image_classes || 'avatar' %>
<% else %>
  <img alt="Image" src="/img/avatar-male-1.jpg" class="<%= image_classes || 'avatar' %>" />
<% end %>